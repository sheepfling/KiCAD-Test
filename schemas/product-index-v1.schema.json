{
  "$defs": {
    "ProductIndexEntry": {
      "additionalProperties": false,
      "properties": {
        "id": {
          "minLength": 1,
          "pattern": "^[A-Za-z0-9][A-Za-z0-9_.-]*$",
          "title": "Id",
          "type": "string"
        },
        "path": {
          "minLength": 1,
          "title": "Path",
          "type": "string"
        },
        "project_ids": {
          "items": {
            "minLength": 1,
            "pattern": "^[A-Za-z0-9][A-Za-z0-9_.-]*$",
            "type": "string"
          },
          "title": "Project Ids",
          "type": "array"
        }
      },
      "required": [
        "id",
        "path",
        "project_ids"
      ],
      "title": "ProductIndexEntry",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "properties": {
    "products": {
      "items": {
        "$ref": "#/$defs/ProductIndexEntry"
      },
      "title": "Products",
      "type": "array"
    },
    "schema_version": {
      "const": "1",
      "title": "Schema Version",
      "type": "string"
    }
  },
  "required": [
    "schema_version",
    "products"
  ],
  "title": "ProductIndex",
  "type": "object"
}
