{
  "$defs": {
    "TemplateUpgrade": {
      "additionalProperties": false,
      "properties": {
        "breaking": {
          "title": "Breaking",
          "type": "boolean"
        },
        "from_version": {
          "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$",
          "title": "From Version",
          "type": "string"
        },
        "id": {
          "minLength": 1,
          "pattern": "^[A-Za-z0-9][A-Za-z0-9_.-]*$",
          "title": "Id",
          "type": "string"
        },
        "steps": {
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "title": "Steps",
          "type": "array"
        },
        "to_version": {
          "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$",
          "title": "To Version",
          "type": "string"
        }
      },
      "required": [
        "id",
        "from_version",
        "to_version",
        "breaking",
        "steps"
      ],
      "title": "TemplateUpgrade",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "properties": {
    "schema_version": {
      "const": "1",
      "default": "1",
      "title": "Schema Version",
      "type": "string"
    },
    "upgrades": {
      "items": {
        "$ref": "#/$defs/TemplateUpgrade"
      },
      "title": "Upgrades",
      "type": "array"
    }
  },
  "required": [
    "upgrades"
  ],
  "title": "TemplateUpgradesCatalog",
  "type": "object"
}
